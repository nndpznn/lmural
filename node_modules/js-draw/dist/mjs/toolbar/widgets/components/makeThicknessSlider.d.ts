import { ToolbarContext } from '../../types';
declare const makeThicknessSlider: (context: ToolbarContext, onChange: (value: number) => void) => {
    container: HTMLDivElement;
    addTo: (parent: HTMLElement) => void;
    setBounds: (min: number, max: number) => void;
    setValue: (thickness: number) => void;
};
export default makeThicknessSlider;
