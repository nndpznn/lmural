/**
 * Creates a separator element that renders a line and, optionally, a header.
 */
declare const makeSeparator: (header?: string) => {
    addTo: (parent: HTMLElement) => void;
};
export default makeSeparator;
